services:
  db:
    image: mysql:8.0.43
    environment:
      MYSQL_ROOT_PASSWORD: manager
      MYSQL_DATABASE: payments
      MYSQL_USER: root
      MYSQL_PASSWORD: manager
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: